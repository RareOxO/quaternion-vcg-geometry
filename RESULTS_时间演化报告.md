# Quaternion Temporal Evolution：角度表示的时间演化

依据 `Quaternion_Temporal_Evolution_快速验证_Plan.docx`。
PTB-XL，fold 1–8 / 9 / 10，阈值 0.5，**seed 42**。

验证 Quaternion 能否作为 **3D angular temporal evolution 的核心表示**（而非网络算子）。
本轮不比较 QuaternionConv vs RealConv——三个模型共用同一个普通实值时间编码器。

---

## 1. 设置

**首次使用真正的旋转四元数。** 此前所有轮次用的都是 full-angle 描述子
`[dot, cross] = [cos θ, n sin θ]`，并明确拒绝称其为旋转四元数；本轮的

```
q_t = Rotation(u_t → u_{t+Δ}) = [cos(θ/2), n sin(θ/2)]
```

是标准 half-angle 形式，因此旋转群中的复合才有意义。

| Variant | Angular 输入 | 通道 | Angular 参数 | 总参数 |
|---|---|---|---|---|
| A0 Local only | `q_t` | 4 | 79,508 | 168,025 |
| A1 Real evolution | `q_t`, `q_{t+τ} − q_t` | 8 | 80,388 | 168,905 |
| A2 Quaternion evolution | `q_t`, `q_t⁻¹ ⊗ q_{t+τ}` | 8 | 80,388 | 168,905 |

**A1 与 A2 参数量完全相同**，前 4 个角度通道是逐比特相同的 `q_t`，
唯一差别是后 4 个通道如何组合同一对操作数：**R⁴ 中的减法 vs 旋转群中的复合**。
A0 仅 stem 相差 880（0.52%）。τ = Δ = 20 ms 固定（§8 禁止同时搜索 Δ、τ、RF）。
R、L 分支、融合、头、感受野（640 samples / 1280 ms）、训练配方全部一致。

### 数学实现（§5 前置检查，全部通过）

`q_t` 由 `[1 + u·v, u×v]` 归一化构造，无三角函数——因其模长恰为 `2cos(θ/2)`。
标量部 `1 + u·v` 恒非负，故 **`w ≥ 0` 的符号约定自动满足**，不会出现人为的时间跳变。
反向情形（θ→π）两部分同时坍缩为零，此时旋转为绕任意垂直轴的 π，代码从与 `u`
最不对齐的坐标轴确定性地选取一个。

验证项：半角形式正确（`x→y` 给出 `[cos45°,0,0,sin45°]`）；`q ⊗ [0,u] ⊗ q*` 精确
将 `u` 送到 `v`；转轴与两端点均正交（最小旋转）；单位模误差 < 1e-5；
`q_t ⊗ e_t = q_{t+τ}`（`e_t` 确为相对旋转的硬证据）；反向情形有限且梯度稳定。

**术语纪律（§5 末条）**：`e_t = q_t⁻¹ ⊗ q_{t+τ}` 是真实的相对旋转，表达在 `q_t`
自身的参考系中；`q_{t+τ} − q_t` 是 R⁴ 中的坐标差，无旋转群含义。测试显式断言前者
模长为 1、后者偏离 1 超过 0.1，二者在代码与报告中从不互换表述。

---

## 2. 结果

| Model | Macro AUROC | NORM | MI | STTC | CD | HYP |
|---|---|---|---|---|---|---|
| **A0 Local only** | **0.9170** | .9424 | .9168 | .9270 | .9104 | .8884 |
| A1 Real evolution | 0.9165 | .9431 | .9158 | .9277 | .9022 | .8939 |
| A2 Quaternion evolution | 0.9134 | .9387 | .9125 | .9229 | .9051 | .8877 |

| 对比 | 问题 | Δ |
|---|---|---|
| **A2 − A1** | Quaternion 复合 vs 普通差分 | **−0.0032** |
| **A1 − A0** | 时间演化本身是否有增量 | **−0.0005** |
| A2 − A0 | 总计 | −0.0036 |

### 逐类 A2 − A1

| NORM | MI | STTC | CD | HYP |
|---|---|---|---|---|
| −0.0044 | −0.0033 | −0.0048 | **+0.0029** | −0.0062 |

---

## 3. 分析

### 3.1 A2 − A1 = −0.0032：判定不可区分，但方向一致为负

按 §6 第二行，落在 0.005 筛查带内 → Quaternion-specific value 不可区分。

但其形态与上一轮（角度算子）的噪声**不同**：上一轮逐类差符号混杂（2 正 3 负）
且相互抵消，macro 差仅 0.0007；本轮 **4/5 为负**，幅度 −0.0033 ~ −0.0062，
不抵消而是同向累积。虽按规格只能判「不可区分」，但**方向上没有任何支持
旋转群复合的迹象**。

### 3.2 A1 − A0 = −0.0005：显式时间演化项毫无贡献

这是本轮更重要的发现，且它与上一轮 temporal context 的正向结果**不矛盾，
反而互相解释**：

| | 变量 | 结论 |
|---|---|---|
| 上一轮 | 编码器**感受野** 90 / 380 / 1280 ms | 看得更久更好（+0.0093） |
| 本轮 | RF **固定 1280 ms**，变是否**显式喂入**演化项 | 无增量（−0.0005） |

A0 的编码器本就拥有 1280 ms 上下文，完全可以自行从 `q_t` 序列卷出任何时间差分；
再手工递给它 `q_{t+τ} − q_t` 或 `q_t⁻¹ ⊗ q_{t+τ}` 属于冗余。

**模型受益于「看得更久」，而非「被喂预计算的时间差」。**

### 3.3 一个附带对照（两份规格均未要求，但恰好完美匹配）

本轮 A0 与上一轮 RLA-Standard 的参数量**完全相同**（angular 79,508 / 总 168,025），
架构、RF、R/L 分支、融合头全部一致，**唯一差别是那 4 个角度通道的定义**：

| Angular 表示 | Macro AUROC |
|---|---|
| RLA-Standard：full-angle `[dot, cross]` | 0.9130 |
| **A0：half-angle 旋转四元数 `q_t`** | **0.9170** |
| | **+0.0040** |

+0.0040 仍在 0.005 带内、两者均为 seed 42 单点，**不构成结论**。但这是本项目首次
出现「半角旋转四元数优于全角描述子」的迹象，且是在完全匹配条件下测得。

若后续仍要在 Quaternion 方向投入，**这个对照比 A2−A1 更值得补 seed**——它测的是
**表示本身**，而 A1/A2 测的是在已有充足时间上下文时**再加演化项**（现已证明冗余）。
此项属规格外观察，§8 禁止本轮扩展，仅作记录。

---

## 4. 结论（<250 字）

在 A1/A2 参数量完全相同、前 4 个角度通道逐比特一致、R/L 分支与融合头相同、
RF 均为 1280 ms 的严格对照下：

**A2 − A1 = −0.0032**，落在 0.005 筛查带内 → Quaternion-specific rotation
composition 与普通时间差分**不可区分**；逐类 4/5 为负，方向上无任何正向迹象。
按 §6，不将 quaternion representation 包装为必要环节，不继续堆叠 Quaternion 架构，
**不进入 §7 的 cardiac-cycle 可解释性分析**（该节以 A2 出现正信号为前提）。

**A1 − A0 = −0.0005**，显式时间演化项无增量。结合上一轮「更长感受野有效
（+0.0093）」，可得一个自洽结论：模型受益于**看到更多时间**，而非被喂
**预计算的时间差**——1280 ms 的编码器本就能自行卷出所需差分。

§9 的证据链在第二环即中断，未能推进至第三、四环。

---

## 5. 证据链现状

```
Instantaneous angular state → Temporal angular evolution → Quaternion-specific 3D evolution → Disease interpretation
        A0 = 0.9170               A1 = 0.9165 (−0.0005)         A2 = 0.9134 (−0.0032)             未触发
                                        ✗ 无增量                       ✗ 无增量
```

Quaternion 假设至此已接受**四次独立检验**，无一次出现正信号：

| 检验 | 性质 | Δ |
|---|---|---|
| 一期 M2 − M1 | 算子，取代框架，3 seed | −0.0012 |
| 融合 F2 − F1 | 算子，叠加框架，3 seed | +0.0003 |
| 角度算子 Quat − Std | 算子，分支+参数对齐，1 seed | −0.0007 |
| **本轮 A2 − A1** | **表示，参数完全相同，1 seed** | **−0.0032** |

前三次测的是 **Hamilton 算子**，本轮测的是 **rotation-group 表示**——两个不同层面，
结论一致。

---

## 附：本轮约束核对

未使用 QuaternionConv；未新增 Quaternion layer；未堆叠 multi-scale / second-order；
未同时搜索 Δ、τ、RF；未改变 R/L/fusion；未作任何 SO(3) invariance/equivariance 主张；
未依据 test fold 调参；未自动补 seed；未自动进入 §7 可解释性分析。
M0–M4、M0-Wide、F1–F4、R/RA/RU/RLA、temporal 与 angular 系列的定义与 checkpoint
均未改动（旧 checkpoint 仍可 `strict=True` 加载，`tables.md` 逐字节一致）。
