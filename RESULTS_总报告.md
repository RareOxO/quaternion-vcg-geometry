# Quaternion-VCG 显式几何：PTB-XL 完整结果报告

两份指导书的合并结论。所有数字来自同一台机器（RTX 4090）上的同一份缓存，
3 个 seed（42/43/44），test fold 10，阈值固定 0.5。

- 一期：`Quaternion_VCG_动态几何_实现方案_公式排版修订版.md` → M0–M4
- 二期：`Raw_VCG_Geometry_双分支融合实验_Codex指令.docx` → M0-Wide、F1–F4

代码 `qdg/`，表格由 `qdg/experiments.py` 自动生成（`runs/tables.md`、`runs/fusion_tables.md`）。

---

## 1. 每个 variant 是什么

所有模型共用同一 backbone：stride-5 stem → 4 个残差块（kernel 5，块间 avg-pool 2）
→ 时间维平均 → 线性头，感受野 605 点（1.21 s）。**只有输入表示和交互规则被消融。**

### 一期：几何**替换** raw（指导书 §8）

| Variant | 输入 | 算子 | 通道 |
|---|---|---|---|
| **M0** | raw VCG `[X,Y,Z]`（保留模长） | Real Conv | 3 |
| **M1** | 20 ms 一阶描述子 `[dot, cross]` | Real Conv | 4 |
| **M2** | **与 M1 数值逐比特相同** | Quaternion Conv | 4 |
| **M3** | 10/20/40/80 ms 一阶 | Quaternion Conv | 16 |
| **M4** | M3 + 各尺度二阶差分 | Quaternion Conv | 32 |

一阶描述子 `q_t^Δ = [u_t·u_{t+Δ}, u_t×u_{t+Δ}] = [cos θ, n sin θ]`，
其中 `u_t = V_t/(‖V_t‖+eps)`。二阶用差分 `q(u_t,u_{t+Δ}) − q(u_{t−Δ},u_t)`。

附加对照：`M2_s10/s40/s80`（换单尺度）、`M1_mlp`（kernel 5→1，去掉块内时间混合）。

### 二期：几何**叠加**在 raw 之上（指导书 §3–§10）

| Variant | Raw 分支 | Geometry 分支 | 参数 |
|---|---|---|---|
| **M0-Wide** | M0 加宽到 92 通道 | 无 | 342,889 |
| **F1** | M0（不变） | M1 | 342,597 |
| **F2** | M0（不变） | M2 | 345,765 |
| **F3** | M0（不变） | M3 | 347,685 |
| **F4** | M0（不变） | M4 | 350,245 |

双分支后期融合：两支各自保留 stem，各投影到 `fusion_dim=64`，concat → LayerNorm
→ 一层 Linear 到 5 类。**F1–F4 融合机制完全一致**，无 attention / gating / MLP。

---

## 2. Variant 之间的关系

```
                    ┌─ M1 ─ M2 ─ M3 ─ M4        一期：几何取代 raw
   raw VCG          │      (输入逐级嵌套)
   [X,Y,Z] ── M0 ───┤
                    │
                    ├─ M0-Wide                  容量对照：只加宽，不加几何
                    │
                    └─ F1 ─ F2 ─ F3 ─ F4        二期：M0 原样保留 + 几何分支
```

三条关键关系：

**M0 → M1 是替换，不是叠加。** M1 把 `[X,Y,Z]` 整个换成 `[dot,cross]`，模长被
归一化丢弃。所以 **M1−M0 是 replacement gap，不能称作「加入几何的收益」**
（二期指导书 §16 专门强调了这一点）。

**M1→M2→M3→M4 输入逐级嵌套。** M2 与 M1 输入完全相同、只换算子；M3 的前 4 通道
就是 M2 的输入；M4 的前 16 通道就是 M3 的全部。所以相邻差值只动一个变量。

**F_n = M0 + M_n，M0 分支被钉死。** 两支参数不共享，几何侧的任何配置改动都
不会流到 raw 分支（有单元测试强制）。**真正的「加入几何的收益」是 F1−M0**，
扣掉容量因素后是 **F1−M0-Wide**。M0-Wide 的宽度 92 是解出来的，参数量落在
F1 的 +0.09%、F4 的 −2.1% 以内。

---

## 3. 结果

### 表 1　一期：几何取代 raw

| Model | Params | Macro AUROC | NORM | MI | STTC | CD | HYP |
|---|---|---|---|---|---|---|---|
| **M0** | 166,853 | **0.9199±0.0002** | .9431 | .9173 | .9334 | .9132 | **.8925** |
| M1 | 167,173 | 0.8834±0.0015 | .9240 | .8914 | .9122 | .8781 | .8112 |
| M2 | 166,565 | 0.8821±0.0008 | .9233 | .8910 | .9106 | .8712 | .8145 |
| M3 | 168,485 | 0.8806±0.0011 | .9229 | .8885 | .9091 | .8839 | .7983 |
| M4 | 171,045 | 0.8833±0.0012 | .9252 | .8933 | .9127 | .8813 | .8042 |

### 表 2　尺度与算子对照

| 变体 | Macro AUROC | 说明 |
|---|---|---|
| M2 @10 ms | 0.8812±0.0012 | |
| M2 @20 ms | 0.8821±0.0008 | 最佳单尺度 |
| M2 @40 ms | 0.8792±0.0007 | |
| M2 @80 ms | 0.8777±0.0017 | 最差单尺度 |
| M3 四尺度合并 | 0.8806±0.0011 | 不及最佳单尺度 |
| M1 Real Conv (167k) | 0.8834±0.0015 | |
| M2 Quaternion Conv (167k) | 0.8821±0.0008 | 与 Real 重叠 |
| M1_mlp Real MLP (36k) | 0.8596±0.0029 | 去掉时间混合 |

### 表 3　二期：几何叠加在 raw 之上

| Model | 几何通道 | Params | Macro AUROC | Δ vs M0 |
|---|---|---|---|---|
| **M0** | 0 | 166,853 | **0.9199±0.0002** | — |
| M0-Wide | 0 | 342,889 | 0.9203±0.0017 | +0.0004 |
| F1 | 4 | 342,597 | 0.9182±0.0022 | −0.0017 |
| F2 | 4 | 345,765 | 0.9185±0.0013 | −0.0014 |
| F3 | 16 | 347,685 | 0.9171±0.0009 | −0.0028 |
| F4 | 32 | 350,245 | 0.9161±0.0031 | −0.0038 |

### 表 4　组件增量

| 对比 | 含义 | Δ Macro AUROC |
|---|---|---|
| M1 − M0 | geometry-only **replacement gap** | −0.0365 |
| M2 − M1 | Quaternion 交互（替换框架） | −0.0012 |
| M3 − M2 | 多尺度（替换框架） | −0.0016 |
| M4 − M3 | 二阶（替换框架） | +0.0028 |
| **F1 − M0** | **Real geometry complementarity** | **−0.0017** |
| **F1 − M0-Wide** | **扣除容量后的几何贡献** | **−0.0021** |
| F2 − F1 | Quaternion 增量（叠加框架） | +0.0004 |
| F3 − F2 | 多尺度增量（叠加框架） | −0.0015 |
| F4 − F3 | 二阶增量（叠加框架） | −0.0010 |

---

## 4. 分析

### 4.1 几何不能取代 raw：−0.0365，约 18 倍 seed 标准差

M0 的 seed 标准差只有 0.0002，三个 seed 几乎重合，所以这个差距不存在解释空间。
损失高度集中在 **HYP（.8925 → .8112，−8.1 点）**，是其它类的 3–4 倍。
机制明确：HYP 的诊断标准（Sokolow-Lyon、Cornell）本质是电压准则，而
`u = V/‖V‖` 把模长整个归一化掉了。这是一期指导书 §2「只研究方向」的设计代价，
不是实现缺陷。

### 4.2 几何也不能补充 raw，而且加得越多越差

```
几何通道数   Δ vs M0
F1   4      −0.0017
F2   4      −0.0014
F3  16      −0.0028
F4  32      −0.0038
```

单调的剂量-反应关系：两个 4 通道模型都在 −0.0015 附近，16 通道 −0.0028，
32 通道 −0.0038。几何分支在这里等同于一条不含信号的干扰输入——融合头必须学会
忽略它，通道越多越忽略不干净。**F4 是全场最差。**

### 4.3 容量已被排除

M0-Wide 参数翻倍（167k → 343k）只换来 **+0.0004**。这同时否掉了两种辩解：
M0 赢不是因为参数分配得巧，F1–F4 输也不是因为容量不够。模型根本不受容量限制。

### 4.4 Quaternion 交互无可测量效应

两个框架给出一致答案：替换框架 M2−M1 = −0.0012，叠加框架 F2−F1 = +0.0004，
都在噪声内。表 2 的对照最干净——M1 与 M2 输入**逐比特相同**、参数量差 0.4%、
其余一切相同，唯一区别是 `nn.Conv1d` 与 Hamilton 分块耦合，结果
0.8834±0.0015 vs 0.8821±0.0008，误差棒完全重叠。

注意这不等于「四元数层没用」：它等价于说，在 4 维输入、167k 参数、这个数据规模
下，Hamilton 权重共享既没帮助也没损害。四元数层用 1/4 权重达到同样效果，在
参数受限场景仍有价值，但本实验是参数量对齐的，测不出这一点。

### 4.5 多尺度与二阶都没有互补性

单尺度里 20 ms 最好（0.8821）、80 ms 最差（0.8777），四尺度合并 0.8806 反而
**不及最佳单尺度**——合并把最好和最差的尺度平均掉了。数值检查已排除「通道冗余」
这一解释：四个尺度的 dot 均值分别是 0.92/0.81/0.59/0.35，信息明显不同。
一期禁用了 scale attention，模型无法学习加权选择，这可能是直接原因。

二阶在替换框架里 +0.0028、叠加框架里 −0.0010，符号相反、幅度都在噪声量级。

### 4.6 HYP 线索的证伪

Stage A 时 F1 的 HYP 是唯一正向（+0.0040），当时标注为「仅 1 个标准差，需更多证据」。
补完梯子后：

```
F1 .8965 (+0.0040)  →  F2 .8944 (+0.0019)  →  F3 .8941 (+0.0016)  →  F4 .8925 (+0.0000)
```

随几何增多单调衰减到零，F4 与 M0 完全相同。若几何真为 HYP 补充了模长以外的
方向信息，F3/F4 应保持或增强。**结论：噪声，不是效应。** 旁证是 M0-Wide 自己
就有 +0.0031——这个量级的 HYP 波动纯宽度变化即可产生。

### 4.7 一个正面结果

M1_mlp（kernel 5→1）为 0.8596，比 M1 低 2.4 点，证明时间维卷积在起实质作用，
backbone 不是摆设，上述零效应不是「模型没学到东西」导致的。
但该行参数量仅 36,101，算子与容量两个变量混在一起，不能单独归因于时间混合；
其收敛需 47 epoch（其余模型 9–17），符合容量受限特征。

---

## 5. 结论

| 问题 | 框架 | 答案 | 证据强度 |
|---|---|---|---|
| 显式几何能否**取代** raw XYZ | 一期 | **否**（−0.0365） | 决定性，约 18× std |
| 显式几何能否**补充** raw XYZ | 二期 | **否**（−0.0017 ~ −0.0038） | 单调恶化，容量已排除 |
| Quaternion 交互优于 Real | 两者 | **无差异**（−0.0012 / +0.0004） | 输入相同、参数对齐，对照干净 |
| 多尺度优于单尺度 | 两者 | **否**（−0.0016 / −0.0015） | 不及最佳单尺度 |
| 二阶提供额外信息 | 两者 | **否**（+0.0028 / −0.0010） | 符号相反，噪声量级 |

两条独立路径——取代与叠加——给出一致的否定结论，且容量对照排除了唯一的
替代解释。一期中五个几何模型全部锁死在 0.8806–0.8834，二期中融合模型随几何
通道数单调退化：瓶颈在 `V → u` 这一步就已形成，后续所有结构改动都在一个已被
压缩的信息空间内进行。

从研究角度，这是一组严格对照下的清晰否定结果。Quaternion 的零效应尤其干净——
输入逐比特相同、参数量对齐、单一变量——这类结论在四元数网络文献中并不常见。

---

## 6. 后续方向（两份指导书均划为范围外）

按预期收益排序，均未实现：

1. **恢复模长信息** —— 独立 radial 通道 `‖V_t‖`，或 reliability gate `ρ/(ρ+τ)`
   （VCG 模长 p01 仅 0.014 mV，等电位段方向本质是噪声）。HYP 上 8.1 点的缺口直指于此。
2. **scale attention** —— 多尺度失效可能源于固定平均，让模型学习尺度权重是自然下一步。
3. **外部验证** —— Chapman–Shaoxing（PhysioNet `ecg-arrhythmia/1.0.0`，WFDB、
   12 导联 500 Hz 10 s，与 PTB-XL 同构）。

---

## 附：复现

```bash
conda activate quaternion_py3xx
python -m qdg sanity                                  # §6 理论检查（12 项）
python -m pytest -q                                   # 93 项单元测试
python -m qdg prepare                                 # 构建缓存
python -m qdg suite --seeds 42 43 44                  # 一期 M0-M4 + 对照
python -m qdg suite --stage fusion --seeds 42 43 44   # 二期 M0-Wide + F1-F4
python -m qdg tables --root runs                      # 生成全部表格
```
