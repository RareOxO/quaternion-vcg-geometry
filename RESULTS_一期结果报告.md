# Quaternion-VCG 动态几何：一期 PTB-XL 验证结果报告

实现依据：`Quaternion_VCG_动态几何_实现方案_公式排版修订版.md`
代码：`qdg/`　结果：`runs/tables.md`、`runs/results.csv`、`runs/results.json`
日期：2026-09-14　环境：`quaternion_py3xx`，PyTorch 2.8.0+cu126，RTX 4060 Laptop

---

## 0. 一句话结论

**显式方向几何在 PTB-XL 五分类上显著劣于 raw XYZ VCG（−0.0373 Macro AUROC，约 28 倍 seed 标准差）；在几何表示内部，Quaternion 交互、多尺度、二阶项三者的增量全部落在噪声量级（|Δ| ≤ 0.0003）。**

方案 §12 明确写了「不要默认 Quaternion 一定优于 Real」，本次结果就是这条警告的实测印证。

---

## 1. 实验设置

### 1.1 数据

PTB-XL 1.0.3，12 导联 / 500 Hz / 10 s，带通 0.5–100 Hz（4 阶 Butterworth，零相位 `sosfiltfilt`）。
标签为五个诊断超类 NORM / MI / STTC / CD / HYP，multi-label。

| 划分 | fold | 记录数 | 患者数 |
|---|---|---|---|
| 训练 | 1–8 | 17,084 | 14,823 |
| 验证 | 9 | 2,146 | 1,917 |
| 测试 | 10 | 2,158 | 1,877 |

21,388 条记录至少含一个超类（411 条无超类被剔除）。使用官方 `strat_fold`，未重新划分；已校验同一患者不跨 fold。

### 1.2 训练配置（五个模型完全一致）

| 项 | 值 |
|---|---|
| 损失 | `BCEWithLogitsLoss`，`pos_weight` 由训练折统计 |
| 优化器 | AdamW，lr 1e-3，weight decay 0.01 |
| 调度 | CosineAnnealing（T_max=60） |
| batch / 裁剪 | 32 / grad-norm 1.0 |
| 早停 | patience 12，监控 fold 9 Macro AUROC |
| 精度 | AMP fp16（几何计算强制 fp32） |
| seed | 42 / 43 / 44，共 9 实验 × 3 seed = 27 次训练 |

模型选择只看 fold 9；fold 10 在 `evaluate` 中读取一次，阈值固定 0.5。

### 1.3 网络

五个模型共用同一 backbone：stride-5 stem → 4 个残差块（kernel 5，块间 avg-pool 2）→ 时间维平均 → 实数线性头。
感受野 605 采样点（1.21 s）。**只有输入特征块和交互规则被消融，其余完全相同。**

参数量对齐：四元数卷积 Q 通道为 `4Q²K` 权重，实数卷积宽度 C 为 `C²K`，取 `real_width = 2×quaternions` 使两者权重数严格相等。

| 模型 | 输入 | 通道 | 算子 | 参数量 |
|---|---|---|---|---|
| M0 | raw VCG `[X,Y,Z]` | 3 | Real Conv | 166,853 |
| M1 | 20 ms 一阶 `[dot, cross]` | 4 | Real Conv | 167,173 |
| M2 | **与 M1 数值完全相同** | 4 | Quaternion Conv | 166,565 |
| M3 | 10/20/40/80 ms 一阶 | 16 | Quaternion Conv | 168,485 |
| M4 | M3 + 各尺度二阶差分 | 32 | Quaternion Conv | 171,045 |

---

## 2. 实现要点与正确性验证

### 2.1 三个必须区分的量

| 函数 | 值 | 说明 |
|---|---|---|
| `pure_quaternion_product(u,v)` | `[−u·v, u×v]` | 纯四元数 Hamilton 积的原始结果 |
| `relation_descriptor(u,v)` | `[+u·v, u×v]` | **本研究使用的 descriptor** |
| 旋转四元数 | `[cos(θ/2), n sin(θ/2)]` | **不是**本研究的对象 |

descriptor 为 `[cos θ, n sin θ]`，是全角量，因此代码与文档中一律不称其为 physical rotation quaternion（方案 §4.2）。

二阶采用差分 `s_t = q(u_t, u_{t+Δ}) − q(u_{t−Δ}, u_t)`，**未**使用 `inverse(q_prev) ⊗ q_next`（方案 §5.2）。

尺度以毫秒声明，按 `round(lag_ms·fs/1000)` 换算；500 Hz 下 10/20/40/80 ms → 5/10/20/40 采样点。不同尺度作为**独立的四元数通道**，未塞入 r/i/j/k。

重新归一化为配置项 `model.renormalize`（默认 `false`），不静默执行（方案 §4.3）。

### 2.2 方案 §6 理论 sanity checks —— 12 项全部通过

`python -m qdg sanity` 输出 expected vs actual：

| 测试 | 期望 | 实测 | 最大误差 |
|---|---|---|---|
| Hamilton `i⊗j` | `[0,0,0,1]` | `[0,0,0,1]` | 0 |
| 原始纯积 `[x,x]` | `[−1,0,0,0]` | `[−1,0,0,0]` | 0 |
| 90° 旋转 | `[0,0,0,1]` | `[0,0,0,1]` | 0 |
| 反向 | `[−1,0,0,0]` | `[−1,0,0,0]` | 0 |
| 恒定方向 一阶 | `[1,0,0,0]` ×50 | 一致 | 0 |
| 恒定方向 二阶 | `0` ×50 | 一致 | 0 |
| 匀速旋转 一阶恒定 | `[cos ωΔ, 0,0, sin ωΔ]` | 一致 | 2.4e-07 |
| 匀速旋转 二阶 | `0` | 一致 | 2.8e-07 |
| 加速旋转 一阶变化 | spread > 1e-3 | 0.0455 | — |
| 加速旋转 二阶非零 | max > 1e-4 | 0.0100 | — |
| `‖q_first‖ = 1` | ≤ 1e-5 | 2.4e-07 | 2.4e-07 |

另有 55 个单元测试全部通过，`ruff check` / `ruff format` 干净。

### 2.3 真实波形上的数值检查

`python -m qdg check-geometry`（512 条训练记录）：

| 尺度 | lag | `‖q‖` 最大偏差 | dot 均值 | `‖cross‖` 均值 |
|---|---|---|---|---|
| 10 ms | 5 | 7.1e-05 | 0.9164 | 0.2280 |
| 20 ms | 10 | 5.9e-05 | 0.8069 | 0.3421 |
| 40 ms | 20 | 4.6e-05 | 0.5937 | 0.4619 |
| 80 ms | 40 | 4.0e-05 | 0.3473 | 0.5803 |

模长偏差 ≤ 7e-5，**默认不重新归一化是站得住的**。dot 均值随尺度单调下降、cross 模长单调上升，符合「尺度越长夹角越大」的物理预期，说明四个尺度确实携带不同信息——这一点很重要，它排除了「多尺度无效是因为通道冗余」这一解释。

VCG 模长分布：p01 = 0.014 mV，p50 = 0.085 mV，p99 = 1.340 mV。

---

## 3. 结果

### Table 1 主结果（test fold 10，阈值 0.5，3 seed 均值±标准差）

| Model | Geo | Quat | MS | 2nd | Macro AUROC | NORM | MI | STTC | CD | HYP |
|---|---|---|---|---|---|---|---|---|---|---|
| **M0** | ✗ | ✗ | ✗ | ✗ | **0.9199±0.0004** | .9418 | .9179 | .9326 | .9130 | **.8941** |
| M1 | ✓ | ✗ | ✗ | ✗ | 0.8826±0.0017 | .9239 | .8914 | .9114 | .8768 | .8093 |
| M2 | ✓ | ✓ | ✗ | ✗ | 0.8829±0.0013 | .9233 | .8888 | .9106 | .8752 | .8164 |
| M3 | ✓ | ✓ | ✓ | ✗ | 0.8826±0.0006 | .9230 | .8871 | .9098 | .8860 | .8068 |
| M4 | ✓ | ✓ | ✓ | ✓ | 0.8824±0.0010 | .9239 | .8912 | .9117 | .8854 | .7997 |

Macro AUPRC：M0 0.8061±0.0012，M1 0.7300±0.0039，M2 0.7288±0.0026，M3 0.7261±0.0012，M4 0.7274±0.0032（同一结论）。

### Table 2 时间尺度分析

| Scale | Macro AUROC | NORM | MI | STTC | CD | HYP |
|---|---|---|---|---|---|---|
| 10 ms | 0.8817±0.0007 | .9194 | .8865 | .9096 | .8769 | .8158 |
| **20 ms** | **0.8829±0.0013** | .9233 | .8888 | .9106 | .8752 | .8164 |
| 40 ms | 0.8802±0.0026 | .9227 | .8860 | .9106 | .8738 | .8081 |
| 80 ms | 0.8775±0.0014 | .9187 | .8825 | .9068 | .8804 | .7989 |
| 10+20+40+80 ms | 0.8826±0.0006 | .9230 | .8871 | .9098 | .8860 | .8068 |

### Table 3 Real vs Quaternion（输入数值完全相同）

| Representation | Operator | Params | Macro AUROC |
|---|---|---|---|
| dot+cross | Real Conv | 167,173 | 0.8826±0.0017 |
| dot+cross | Real MLP | 36,101 | 0.8592±0.0049 |
| dot+cross | Quaternion Conv | 166,565 | 0.8829±0.0013 |

### Table 4 组件增量

| Component Added | Δ Macro AUROC | 相对 seed std (~0.0013) |
|---|---|---|
| Explicit Geometry (M1−M0) | **−0.0373** | ≈ 28× std，决定性 |
| Quaternion Interaction (M2−M1) | +0.0003 | 噪声内 |
| Multi-scale (M3−M2) | −0.0003 | 噪声内 |
| Second-order (M4−M3) | −0.0001 | 噪声内 |

---

## 4. 分析

### 4.1 Q1：显式几何是否优于 raw XYZ VCG？—— 否，且幅度决定性

−0.0373 是 seed 标准差的约 28 倍，三个 seed 方向完全一致，不存在「再多跑几个 seed 可能翻盘」的空间。

按类别拆解 M1 相对 M0 的损失：

| | NORM | MI | STTC | CD | HYP |
|---|---|---|---|---|---|
| M1 − M0 | −0.0179 | −0.0265 | −0.0212 | −0.0363 | **−0.0848** |
| M4 − M0 | −0.0178 | −0.0267 | −0.0209 | −0.0276 | **−0.0944** |

**损失高度集中在 HYP（−8.5 ~ −9.4 点），是其它类别的 3–5 倍。** 这不是随机分布的退化，而是有明确机制的：HYP（心室肥厚）的判读依据本质上是 QRS 振幅（Sokolow-Lyon、Cornell 等诊断标准都是电压准则），而 `u_t = V_t/(‖V_t‖+eps)` 把模长整个归一化掉了。CD（传导阻滞）依赖 QRS 时限与形态，部分信息保留在方向变化里，所以损失居中；NORM/STTC 损失最小。

这是方案 §2「第一阶段只研究方向随时间的变化」的**设计代价，不是实现缺陷**。M1–M4 的输入在信息量上严格少于 M0，结果符合这一事实。

### 4.2 Q2：Quaternion 结构化交互是否优于实数交互？—— 无可测量差异

Table 3 是本报告最干净的对照：

- 输入数值 **bit-identical**（单元测试 `test_m1_and_m2_receive_identical_input` 强制验证）
- 参数量 167,173 vs 166,565（差 0.4%）
- backbone、深度、感受野、优化器、seed 全部相同
- 唯一区别是 `nn.Conv1d` 与 `QuaternionConv1d` 的 Hamilton 分块耦合

结果 0.8826±0.0017 vs 0.8829±0.0013，误差棒完全重叠，Δ = +0.0003。**在这个任务上，Hamilton 代数约束没有带来可测量的归纳偏置收益。**

需要说明的是这不等于「四元数层没用」——它等价于说：在 4 维输入、167k 参数、这个数据规模下，权重共享带来的约束既没有帮助也没有损害。四元数层用 1/4 的权重达到同样效果，这在参数受限场景下仍有价值，但本实验设计是参数量对齐的，测不出这一点。

### 4.3 Q3：多尺度是否优于单尺度？—— 否

四尺度合并 0.8826±0.0006 与单独 20 ms 的 0.8829±0.0013 打平。Table 2 内部有一个弱但一致的排序：20 ms ≈ 10 ms > 40 ms > 80 ms，80 ms 单独使用明显最差（0.8775）。

§2.3 的数值检查已经排除了「通道冗余」这一解释——四个尺度的 dot 均值分别是 0.92/0.81/0.59/0.35，信息明显不同。更可能的解释是：**合并操作把最好的尺度和最差的尺度平均了**，增益与稀释相互抵消。方案一期禁用了 scale attention，模型无法学会加权选择尺度，这可能是多尺度失效的直接原因。

值得注意的是 M3 在 CD 上是所有模型里最好的（.8860，M2 为 .8752，+1.1 点），说明多尺度对传导类异常确有帮助，只是被 HYP 上的退化（.8164 → .8068）抵消掉了。

### 4.4 Q4：二阶几何是否提供一阶之外的信息？—— 否

Δ = −0.0001。M4 的 HYP 是所有几何模型中最低的（0.7997）。二阶差分在增加 16 个输入通道的同时没有带来任何增益。

### 4.5 一个正面结果：backbone 确实在工作

Table 3 中 Real MLP（kernel 5→1，移除块内时间维混合）为 0.8592±0.0049，比 Real Conv 低 2.3 点。这证明时间维卷积在起实质作用，backbone 不是摆设，前述零效应不是「模型根本没学到东西」导致的。

**但这一行参数量只有 36,101（kernel 从 5 降到 1 直接砍掉 4/5 权重），算子变化与容量变化混在一起，不能单独归因于「时间混合」。** Table 3 保留 Params 列正是为此。另一个佐证是 M1_mlp 训练了 47 个 epoch 才达到最优（其它模型为 9–17），符合容量受限模型的收敛特征。

### 4.6 训练过程观察

所有 run 在 21–29 epoch 早停（M1_mlp 为 52），最优 epoch 落在 9–17。这意味着 **CosineAnnealing 的 T_max=60 实际从未跑完**，学习率只从 1e-3 退火到约 6.6e-4，模型没有进入低学习率精调阶段。

这不影响对比的公平性（五个模型同一套 recipe，同样受影响），但绝对数值可能比这套配置的上限低若干千分点。若要复现更高的绝对分数，可将 `training.epochs` 降至 30 让余弦调度完整执行。

M0 的典型曲线：train loss 单调下降 0.69 → 0.37，val AUROC 在 epoch 12 达到 0.9259 后开始退化，早停正确保留了最优权重。val 0.9259 → test 0.9198，gap 0.006，无验证集过拟合。

M0 的 0.9199 与文献中 PTB-XL 五分类（完整 12 导联）的 0.92–0.93 处于同一水平，而本实验只用了 VCG 的三个通道——说明基线是可靠的，不是一个被人为压低的参照物。

---

## 5. 总体判断

一期设定的四个问题全部得到了明确答案，其中三个为否定、一个为无差异：

| 问题 | 答案 | 证据强度 |
|---|---|---|
| Q1 显式几何 > raw XYZ | **否**（−0.0373） | 决定性（28× std） |
| Q2 Quaternion > Real | **无差异**（+0.0003） | 参数对齐、输入相同，对照干净 |
| Q3 多尺度 > 单尺度 | **否**（−0.0003） | 噪声内 |
| Q4 二阶有增量 | **否**（−0.0001） | 噪声内 |

更具诊断价值的观察是：**五个几何模型全部锁死在 0.8824–0.8829，第三位小数都难以区分。** 无论更换算子、增加尺度还是叠加二阶项，都无法撼动这个平台。这说明瓶颈在 `V → u` 这一步就已形成，后续所有结构改动都在一个已被压缩的信息空间内进行。

从研究角度看，这是一组**清晰且可发表的否定结果**，尤其 Q2 的零效应是在输入数值完全相同、参数量严格对齐的条件下测出的，方法学上很干净——这类严格对照的负面结论在四元数网络文献中并不常见。

---

## 6. 后续方向（均超出一期范围，待决策）

一期的禁止事项（方案 §12）限定了可做的范围。若要让几何分支具备竞争力，按预期收益排序：

1. **恢复模长信息**。最小改动是增加独立的 radial 通道 `‖V_t‖`，或采用旧项目 `a_ecg_rotation_dynamics` 中的 reliability gate `ρ/(ρ+τ)`（等电位段上方向本质是噪声，VCG 模长 p01 仅 0.014 mV）。HYP 上 9.4 点的缺口直接指向这里。
2. **scale attention**。Table 2 显示多尺度失效可能源于固定平均，让模型学习尺度权重是自然的下一步。
3. **几何与原始信号融合**。M0 与 M1–M4 的误差模式差异明显（几何在 CD 上有 M3 的局部优势），二者可能互补而非替代。

这三项方案 §12 均明确划归一期之外，本报告不做实现。

---

## 附：复现命令

```bash
conda activate quaternion_py3xx
cd /home/theo2204/python-workspace/Quaternion/q_wyt
pip install -e .

python -m qdg sanity                  # §6 理论检查
python -m pytest -q                   # 55 项单元测试
python -m qdg prepare                 # 构建缓存（约 2 分钟，4.8 GB）
python -m qdg check-geometry          # 真实波形数值检查
python -m qdg suite --seeds 42 43 44  # 27 次训练，约 6 小时
python -m qdg tables --root runs      # 生成四张表
```

所有表格由 `qdg/experiments.py` 从 run 目录自动生成，非手工填写。
