# 可解释性热图结果（Exp 9 / 方案第 9 节）

对象：`runs/E2_RLQ_seed42/best.pt`（完整 R+L+Q 三支模型）。测试集 2158 条，按预设的
R 峰质量规则丢弃 89 条（4.1%），实际分析 **2069 条**；逐病阳性患者数 MI 465 / STTC 470 /
CD 427 / HYP 245（记录先塌缩到患者，再做患者级 bootstrap，1000 次）。

贡献定义 `C = z − z_perturbed`：**正值 = 移除该信息后 logit 下降，即该信息支持该诊断**。
R/L 用线性插值移除，Q 沿测地线（SLERP）在窗口两端旋转之间插值；identity（把窗口内旋转
置为单位四元数）为方案要求的稳健性对照。主图为 w = 20 ms、stride = 10 ms、SLERP。

## 表 1：各病 × 各支路的最大贡献（w20，SLERP）

| 病种 | 支路 | 峰值 C | 位置 | 95% CI 排除 0 | 显著点数 /91 |
|---|---|---|---|---|---|
| MI | R | +0.019 | 0 ms | 是 | 22 |
| MI | **L** | **−0.604** | −10 ms | 是 | 51 |
| MI | Q | −0.125 | 0 ms | 是 | 43 |
| STTC | R | +0.337 | 0 ms | 是 | 53 |
| STTC | **L** | **+0.700** | −10 ms | 是 | 42 |
| STTC | Q | +0.054 | −30 ms | 是 | 20 |
| CD | R | −0.132 | −20 ms | 是 | 41 |
| CD | **L** | **−0.451** | −10 ms | 是 | 25 |
| CD | Q | +0.050 | +10 ms | 是 | 14 |
| HYP | **R** | **+1.025** | 0 ms | 是 | 37 |
| HYP | L | +0.569 | −10 ms | 是 | 15 |
| HYP | Q | +0.025 | 0 ms | 是 | 10 |

## 表 2：支路占比（各病 |C| 之和的百分比）

| 病种 | 全时程 R / L / Q | 仅 QRS 之外（\|t\|>60 ms） R / L / Q |
|---|---|---|
| MI | 2.6 / **66.3** / 31.1 | 1.9 / **60.4** / 37.7 |
| STTC | 24.0 / **58.9** / 17.1 | 17.4 / **47.4** / 35.2 |
| CD | 18.7 / **69.4** / 11.9 | 10.8 / **66.0** / 23.2 |
| HYP | 41.2 / **51.4** / 7.3 | 22.2 / **51.1** / 26.7 |

分相位均值（w20）显示贡献高度集中于 QRS：以 MI 的 L 支为例，QRS（−60…60 ms）均值
−0.0659，而 ST（80…240 ms）−0.0072、T（260…440 ms）−0.0013，相差一个量级。
QRS 之外最大的单个显著窗口：**CD 的 L 在 −190 ms（−0.048）**，即 P–R 区段；
MI 的 Q 在 +300…+460 ms（约 −0.014）；HYP 的 L 在 −200 ms（−0.021）。

## 表 3：稳健性

| 检查 | 结果 |
|---|---|
| 窗宽 10 / 20 / 40 ms 全图相关 | r(10,20)=0.891，r(20,40)=0.756，r(10,40)=0.560 |
| 峰值幅度随窗宽 | max\|C\| = 0.281 / 1.025 / 1.796，近似正比于被移除时长 |
| 分支路 r(10,40) | R 0.545，L 0.617，**Q 0.156** |
| 支路排序（按 Σ\|C\|） | 10/20 ms 全部为 L > R/Q；40 ms 仅 HYP 翻为 R > L（两者本就接近） |
| Q 支 SLERP vs identity | 全图 r=0.804，**仅 Q 行 r=0.368**（MI 0.28 / STTC 0.60 / CD 0.05 / HYP −0.06） |
| identity 的 Q 幅度 | 为 SLERP 的 2.3–7.5 倍，且排序翻转（MI、STTC 变为 Q 居首） |

## 结论

三支的分工在统计上清晰且与临床先验一致：**L（线速度）在四个病种中都是第一贡献支
（51–69%），R（半径/幅度）只在 HYP 上跃居主导（峰值 +1.025，为全图最大值，恰好落在
R 峰 0 ms 处）**——后者正是肥厚诊断所用的 QRS 电压判据（Sokolow-Lyon 一类）在模型内部的
对应物，可作为方法有效性的一个强 sanity check；CD 在 QRS 之外的最大显著窗口位于
**−190 ms 的 P–R 区段**，同样与传导异常的电生理定位吻合。Q（旋转四元数）的绝对贡献
在全时程只占 7–31%，但在 QRS 之外其占比升至 **23–38%**，说明 Q 承载的是 QRS 之外、
幅度较小的方向演化信息，而非峰值信息——这与 Exp 7 中"单独打乱 Q 代价很小（≤0.0059）、
三支联合打乱代价大一个量级（0.0425）"完全一致：Q 的价值在于与 R/L 的**时间对齐**，
而不在于其自身某个时刻的强度。
但必须记录三条限制：（1）贡献几乎全部集中在 QRS 窗内，而 QRS 恰是 |V| 与 dV/dt 最大处，
因此"模型看 QRS"部分是"移除 QRS 对输入改动最大"的必然结果，幅度不可直接当作重要性；
（2）**Q 支的时间定位不稳健**——窗宽 10→40 ms 的相关只有 0.156，SLERP 与 identity 两种
移除方式在 Q 行只有 0.368 的相关（identity 下 STTC 的 Q 在 +100…+350 ms 的 ST-T 段出现一条
SLERP 中看不到的宽带），故**只可陈述 Q 的支路级占比，不可陈述 Q 在某一相位的具体定位**；
（3）+590/+600 ms 处若干病种贡献回升，应视为快心率记录（p95 约 116 bpm，RR≈517 ms）下
该相对偏移已越过下一个 R 峰所致的重叠效应，不作解读。

## 图的一个问题

四个面板共用一个对称色标，而 HYP/R 的 +1.025 是全图唯一的极值，导致其余 11 行几乎全白，
肉眼读不出上面这些结构。建议把色标上限改为全图 |C| 的 99 分位（面板间仍可比），
或改为逐面板归一（面板内可读、面板间不可比）。我倾向前者，一行代码，需要的话我改。
