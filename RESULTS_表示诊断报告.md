# 表示诊断：radial / direction / linear / angular

依据 `Quaternion_VCG_NextValidation_Codex_Instruction_v2.docx`。
PTB-XL，fold 1–8 训练 / 9 验证 / 10 测试，阈值固定 0.5。
全部 Real TCN，固定 20 ms 单尺度，**seed 42**（M0 为已有三 seed 结果，M1 为已有 seed42 结果，均直接复用未重训）。

本轮不是模型竞赛，而是定位「raw XYZ 相比 angular-only 的优势究竟来自哪一步」。

---

## 1. 四个诊断变体

| Variant | 输入 | 通道 | 要回答的问题 |
|---|---|---|---|
| R | `r = ‖V‖` | 1 | magnitude 本身含多少信息？ |
| RA | `r` + 20 ms `[dot, cross]` | 5 | magnitude 能补回 angular-only 的损失吗？ |
| RU | `r` + `u = V/(r+eps)` | 4 | `(r,u)` 是否无损保留 raw XYZ？ |
| RLA | `r` + linear `ΔV` + `[dot, cross]` | 8 | 加上线性动态能恢复更多吗？ |

RU 不是候选模型而是**审计对照**：因为 `V = r·u`，RU 在信息上应等价于 M0。
linear velocity 建在 **raw XYZ** 上（非 `u`）：规格定义 `l = (V_{t+1}−V_t)/dt`，
代码输出 `(V_{t+1}−V_t)/vcg_std`，即该速度除以常数 `Fs·vcg_std`；`r` 除以
`sqrt(Σ vcg_std²)`。两个缩放常数都只来自训练折、且已在现有缓存中，属纯逐轴常数
缩放，不增删任何信息，也不抹掉 amplitude。

---

## 2. 结果

| Model | Representation | Params | Macro AUROC | NORM | MI | STTC | CD | HYP |
|---|---|---|---|---|---|---|---|---|
| M0 | XYZ | 166,853 | 0.9199±0.0003 | .9431 | .9173 | .9334 | .9132 | .8925 |
| M1 | Angular `[dot,cross]` | 167,173 | 0.8841 | .9238 | .8957 | .9136 | .8801 | .8073 |
| R | Radial | 166,213 | 0.8700 | .9089 | .8123 | .9000 | .8517 | .8773 |
| RA | Radial + Angular | 167,493 | 0.9094 | .9371 | .9023 | .9223 | .8971 | .8881 |
| **RU** | Radial + Absolute Direction | 167,173 | **0.9206** | .9448 | .9177 | .9305 | .9113 | .8987 |
| RLA | Radial + Linear + Angular | 168,453 | 0.9186 | .9434 | .9173 | .9260 | .9160 | .8903 |

| 差值 | 问题 | Δ Macro AUROC |
|---|---|---|
| RA − M1 | 加入 magnitude 恢复多少？ | **+0.0253** |
| RU − M0 | `(r,u)` 重参数化的代价 | **+0.0007** |
| RA − RU | 相对角度压缩 vs 绝对方向的代价 | **−0.0112** |
| RLA − RA | 线性动态能否进一步恢复？ | **+0.0092** |
| RLA − M0 | 完整分解距 raw baseline 的差距 | −0.0013 |

噪声带取 0.005（由已完成三 seed 实验实测的 seed 波动 0.0002–0.0031 定出）；
seed=42 单点无标准差，带内一律视为不可区分。

---

## 3. 分析

**损失链条被完整拆开了**，且两段损失都有明确归属：

```
M1  angular only          0.8841
 │  + magnitude                   +0.0253   ← 第一段损失
RA  radial + angular      0.9094
 │  绝对方向替代相对几何            +0.0112   ← 第二段损失
RU  radial + absolute dir 0.9206  ≈ M0 (+0.0007)
```

**`RU − M0 = +0.0007` 是本轮最关键的一个数。** RU 用 `(r,u)` 重参数化 raw XYZ，
理论上无损，实测与 M0 完全打平。这直接排除了「归一化 / 缩放 / 实现有问题」这条
解释路径（§9 第四行），证明 pipeline 本身没有缺陷，此前几轮观察到的性能差距是
**真实的表示损失**，不是工程 bug。

**第二段损失指向的是 `u → [dot,cross]` 这一步本身。** RA 与 RU 输入通道数接近
（5 vs 4）、参数量接近，差别只在于用「20 ms 相对角度关系」还是「绝对方向」来
承载方向信息，结果 RA 低 0.0112。**这正是一期 M1–M4 一直在精雕细琢的那个表示**——
现在有证据表明它本身就是有损压缩，丢的是绝对方向，所以加多尺度、加二阶都救不回来。
这与一期「五个几何模型全部锁死在 0.881–0.883」的现象相互印证。

**HYP 上的机制被直接验证。** 一期报告推测 HYP 的损失源于 `u = V/‖V‖` 丢弃模长
（诊断标准是电压准则）。本轮给出了定量确认：

```
HYP:  M1 .8073  →  RA .8881  (+0.0808)     仅加入 r 这一个通道
      R（只有 r）.8773，已高于 M1 的 .8073
```

**+8.1 点是全部实验中最大的单类效应**，且 `R` 仅用一个 radial 通道就在 HYP 上
超过了用四个角度通道的 M1。假设成立。逐类看，RA−M1 的增益在所有类上均为正
（NORM +.013 / MI +.007 / STTC +.009 / CD +.017 / HYP +.081），但 HYP 是其余的 5–12 倍。

**一个正面发现：三元分解信息近似完备。** `RLA − RA = +0.0092`，`RLA − M0 = −0.0013`。
加入 linear velocity 后，radial + linear + angular 几乎补回 M0 的全部性能，恢复
集中在 MI（+.015）和 CD（+.019）。这是这几轮实验里第一个正向结果。

但需克制表述：−0.0013 落在噪声带内，只能说「**打平**」，不能说「超过」；且 RLA
参数量 168,453 略高于 M0 的 166,853。当前证据支持「该分解未损失信息」，不支持
「该分解更优」。

---

## 4. 结论

**假设判定：partially supported。**

magnitude 丢失确实是主要损失来源（+0.0253，HYP 上 +0.0808），假设的这一半成立；
但 magnitude 补回之后仍有 0.0112 的缺口，而 RU 证明该缺口不在 magnitude 也不在
实现，**而在 `u → [dot,cross]` 的相对几何压缩本身**。

由此得到一条明确的负面指导：**继续在 `[dot,cross]` 上做文章（多尺度、二阶、
Quaternion 算子）没有前途**，因为丢失的是绝对方向信息，这些操作都无法恢复。
若要保留几何动机，方向应是「保留绝对方向的表示」或「radial+linear+angular 三元分解」。

本轮所有变体均为 Real，因此**不能据此对 Quaternion 的有效性下任何结论**。

---

## 5. 第二阶段建议（待确认，未启动）

按 §10，只建议对 **RU 和 RLA** 补 seed 43/44：

- **RU** —— 它是整个诊断的地基（`RU ≈ M0` 支撑了「实现无问题」这一判断），
  值得确认在多 seed 下稳定；
- **RLA** —— 确认「三元分解 ≈ M0」不是单 seed 偶然，并加一个参数匹配的 raw control
  （RLA 参数量高出 M0 约 1%）。

R 与 RA 已完成定位任务，不必再补 seed。

```bash
python -m qdg suite --experiments RU RLA --seeds 42 43 44
```

---

## 附：本轮约束核对

未使用 QuaternionConv；未新增 multi-scale（固定 20 ms）；未新增 second-order；
无 attention / gating / Transformer；未引入外部数据集；未加入 12 导联分支；
未依据 test fold 调整任何超参数；M0–M4、M0-Wide、F1–F4 的定义、checkpoint
与既有表格均未改动（旧 checkpoint 仍可 `strict=True` 加载，`tables.md` 逐字节一致）。
